{
  "compilerOptions": {
    "outDir": "./dist/",
    "noImplicitAny": true,
    "module": "ESNext",
    // Compile to:
    "target": "es5",
    "jsx": "react",
    "allowJs": true,
    "moduleResolution": "node",
    /* When enabled, TypeScript allows you to use default-named objects from CommonJS modules just like they were exported using the standard ES6 syntax. */
    "esModuleInterop": true,
    /* "allowSyntheticDefaultImports" flag in TypeScript is used to allow the import of modules that don't explicitly export a "default".
    It "synthetically" creates a "default" export for modules that don't have one. */
    "allowSyntheticDefaultImports": true
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}